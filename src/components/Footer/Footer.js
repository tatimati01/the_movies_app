import React from 'react';

import css from '../../App.module.css'

const Footer = () => {
    return (
        <div className={css.footer}>
            Some info in Footer
        </div>
    );
};

export default Footer;